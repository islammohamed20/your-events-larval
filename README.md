# Your Events (Laravel)

منصة لإدارة الخدمات، العروض (Quotes)، والحجوزات، مبنية على Laravel.

## التثبيت السريع
- تثبيت الاعتمادات:
  - `composer install`
- إنشاء ملف البيئة:
  - `cp .env.example .env`
  - `php artisan key:generate`
- إعداد قاعدة البيانات داخل `.env` ثم:
  - `php artisan migrate`
- تشغيل محليًا:
  - `php artisan serve`

مزيد من التفاصيل في [docs/README.md](docs/README.md).

## ملاحظات أمنية مهمة
- لا ترفع ملف `.env` إلى GitHub (هو مُدرج ضمن `.gitignore`).
- أي مفاتيح دفع/SMTP يجب أن تبقى داخل `.env` أو Secrets في CI/CD.

## آخر تحديث (2026-03-28)
- إضافة نظام النشرة البريدية (Newsletter) كاملًا: موديل، Controller، Mail، قالب بريد، وجدول قاعدة بيانات مخصص للاشتراكات.
- تحسين مسار الموردين بإضافة Middleware لمنع وصول عملاء المنصة إلى بوابة الموردين بشكل غير مصرح.
- تحديثات على الحجوزات والعروض بإضافة دعم تاريخ الحجز داخل عناصر السلة وعناصر العرض.
- تحسينات على بنية قاعدة البيانات عبر Migrations جديدة، تشمل جعل بعض حقول الدفع قابلة لأن تكون فارغة حسب سيناريو التنفيذ.
- تحسينات إضافية في لوحات الإدارة وواجهات المستخدم وتدفق البريد الإلكتروني المرتبط بالحجز والعروض.
