; Per-directory PHP overrides for file uploads (PHP-FPM)
; Give enough headroom for supplier registration attachments.
upload_max_filesize = 15M
post_max_size = 32M
max_file_uploads = 20
max_input_vars = 3000
